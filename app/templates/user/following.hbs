{{#each user in controller}}
<div class="row">
  <section class="column">
    
    <h5 class="large-8 column friend">{{user.name}}</h5>
    <div class="large-2 column follow"><button>Follow</button></div>
    <hr />
  </section>
</div>
{{/each}}